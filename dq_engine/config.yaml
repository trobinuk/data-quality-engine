tables:
  customers:
    enabled: true
    rules:
      - check_null_customer_name.sql
      - check_duplicate_customer_email.sql
  orders:
    enabled: true
    rules:
      - check_null_order_date.sql